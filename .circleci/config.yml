version: 2.1
orbs:
  dotnet: circleci/dotnet@0.0.44
jobs:
  build:
    executor: dotnet/default
    steps:
      - dotnet restore
      - dotnet build
  
workflows:
  build:
    jobs:
      - build
    

